# Kevin — FIX für Pro-Claude + Free-Claude 🔧

**Problem:** Nach setup-claude funktioniert pro-claude nicht mit Opus, und free-claude nicht mit OpenRouter

**Root Cause:** 
- Model-Format war falsch: `"claude-opus-4-7"` statt `"opus"`
- OpenRouter-Modell war falsch: `"google/gemma-2-9b-it:free"` existiert nicht
- Endpoint war falsch: `/api` statt `/api/v1`

---

## 🚀 FIX — Schritt für Schritt

### Schritt 1: Reset

```bash
kevin@nextkm:~$ delete-claude
# → "ja" eingeben
```

### Schritt 2: Neue Settings vorbereiten

Kopiere die korrigierten Dateien in dein ~/.claude/:

```bash
# Pro-Profil (Opus)
cat > ~/.claude/settings.pro.json << 'EOF'
{
  "theme": "dark",
  "model": "opus",
  "comment": "Pro-Profile: Browser OAuth (Anthropic Claude.ai)"
}
EOF

# Free-Profil (OpenRouter mit kostenloses Llama Model)
cat > ~/.claude/settings.free.json << 'EOF'
{
  "theme": "dark",
  "model": "meta-llama/llama-2-7b-chat:free",
  "comment": "Free-Profile: OpenRouter API Integration",
  "env": {
    "ANTHROPIC_BASE_URL": "https://openrouter.ai/api/v1",
    "ANTHROPIC_AUTH_TOKEN": "sk-or-v1-... dein-api-key-hier ...",
    "ANTHROPIC_API_KEY": ""
  }
}
EOF
```

**WICHTIG:** In settings.free.json: Ersetze `sk-or-v1-... dein-api-key-hier ...` mit deinem echten OpenRouter API Key!

### Schritt 3: Pro-Claude testen

```bash
kevin@nextkm:~$ pro-claude

🚀 Claude Pro aktiviert
   Auth: OAuth (Browser) ✓
   Model: opus
```

Sollte zeigen: **Auth: OAuth (Browser) ✓** und **Model: opus**

Jetzt in Claude Code:
```
❯ /login
  ⎿ Login successful

❯ hello
# Antwortet mit Opus!

❯ exit
```

### Schritt 4: Free-Claude testen

```bash
kevin@nextkm:~$ free-claude

🆓 Claude Free (OpenRouter) aktiviert
   API: OpenRouter ✓
   Endpoint: https://openrouter.ai/api/v1
   Model: meta-llama/llama-2-7b-chat:free
```

Sollte zeigen: **API: OpenRouter ✓** mit **/v1 endpoint**

Jetzt in Claude Code:
```
❯ hello
# Antwortet mit kostenlos Modell von OpenRouter!

❯ exit
```

---

## 🎯 Was wurde repariert

| Problem | Vorher | Nachher |
|---------|--------|---------|
| **Pro-Modell** | `"claude-opus-4-7"` → Claude Code versteht nicht | ✅ `"opus"` → Claude Code erkennt Opus |
| **Free-Modell** | `"google/gemma-2-9b-it:free"` → existiert nicht | ✅ `"meta-llama/llama-2-7b-chat:free"` → Kostenloses Llama Modell |
| **Free-Endpoint** | `/api` → falsch für OpenRouter | ✅ `/api/v1` → korrekt für Claude Code |
| **Auth nach Setup** | OAuth-Tokens nicht erhalten | ✅ settings.pro.json hat korrektes format |

---

## 📝 Checklist

- [ ] `delete-claude` ausgeführt
- [ ] `~/.claude/settings.pro.json` mit `"model": "opus"` erstellt
- [ ] `~/.claude/settings.free.json` mit OpenRouter API Key und `/v1` endpoint erstellt
- [ ] `pro-claude` zeigt "Auth: OAuth (Browser) ✓" und "Model: opus" ✓
- [ ] `/login` in pro-claude erfolgreich ✓
- [ ] `hello` in pro-claude antwortet mit Opus ✓
- [ ] `free-claude` zeigt "API: OpenRouter ✓" und `/v1` endpoint ✓
- [ ] `hello` in free-claude antwortet mit OpenRouter Modell ✓

**Wenn alle Checks ✓: ALLES FUNKTIONIERT!** 🎉

---

## 🔐 OpenRouter API Key

Falls du noch keinen Key hast:
1. Gehe zu: https://openrouter.ai
2. Registriere dich (kostenlos)
3. Gehe zu: https://openrouter.ai/keys
4. Kopiere deinen API Key (beginnt mit `sk-or-v1-...`)
5. Füge den Key in `~/.claude/settings.free.json` ein

---

## ⚡ Schnelle Befehle

```bash
# Alles zeigen
cat ~/.claude/settings.pro.json
cat ~/.claude/settings.free.json

# Pro testen
pro-claude

# Free testen
free-claude

# Zurück zu Pro
pro-claude

# Zurück zu Free
free-claude
```

---

**Das sollte jetzt funktionieren!** 🚀

Falls noch Probleme: `delete-claude` + neu aufbauen mit den richtigen Profilen oben.
